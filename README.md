# LoopSafari – Python Loops & Control Flow Assessment

## Skills practiced
- for / while loops
- conditionals (if-elif-else)
- accumulation patterns
- list transformations
- string formatting & validation
- basic input simulation
- finding maximum streak / peaks / depletion points

## Your task
Complete the functions in `loopsafari.py` so that **all tests pass**.

```bash
# Run all tests
python3 -m pytest tests/test_loopsafari.py -v

# Run one test
python3 -m pytest tests/test_loopsafari.py::TestLoopSafari::test_animal_counting -v
```

## Functions to implement

`count_animals `-> int (total)
Game reserve headcount
for loop

`half_price_tickets` -> list[str] "R 120"
School holiday special
for + transform

`enter_gate_password` -> prints only
Malelane gate boom
while + input


`longest_rainy_days` -> int
Weather station – rainy spell
for + streak

`find_traffic_peaks` -> list[int] peak values
N1 traffic monitoring
for + look-behind

`validate_vehicle_reg` -> dict: valid/invalid
eNaTIS number plate check
for + validation

`water_tank_forecast` -> str message
JoJo tank + daily usage
while / for + early stop




