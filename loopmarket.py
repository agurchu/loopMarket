def total_stock_value(quantities):
    """Return the total number of items in stock."""
    pass


def student_discount_prices(original_prices):
    """Apply 20% student discount and format as 'R 80' strings."""
    pass


def try_unlock_phone(correct_pin):
    """Keep asking for 4-digit PIN until correct (prints messages)."""
    pass


def longest_sunny_streak(weather_log):
    """Return longest streak of consecutive 'SUNNY' days."""
    pass


def find_hot_days(temperatures):
    """Return list of temperatures that are strict local maxima."""
    pass


def check_id_numbers(id_list):
    """
    SA ID = 13 digits, first 6 = YYMMDD, next 4 = gender/seq, next 1 = citizenship, last 1 = check digit
    Very simplified: just check length == 13 and all digits
    Returns {'valid': [...], 'invalid': [...]}
    """
    pass


def airtime_balance_alert(initial_balance, daily_costs):
    """
    Return one of:
    - "Airtime finished on day X."
    - "Airtime lasts all week. Remaining: R Y"
    - "No airtime to start with." (if initial <= 0)
    """
    pass
